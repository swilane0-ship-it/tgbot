# Инструкция по деплою Telegram бота

## ⚠️ Важно: Vercel не подходит для этого бота!

Ваш бот использует `run_polling()` (long-polling), что требует постоянно работающего процесса. 
Vercel - это serverless платформа с ограничениями по времени выполнения (до 60 секунд), 
поэтому для long-polling ботов она **НЕ подходит**.

## ✅ Рекомендуемые платформы (бесплатные)

### 1. Railway (РЕКОМЕНДУЕТСЯ - самый простой)

**Преимущества:** Очень простой, бесплатный tier, автоматический деплой из GitHub

**Шаги:**

1. **Загрузите проект на GitHub** (если еще не загрузили)
   - См. файл `GITHUB_UPLOAD.md`

2. **Создайте аккаунт на Railway**
   - Перейдите на https://railway.app
   - Зарегистрируйтесь через GitHub

3. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

4. **Настройте проект**
   - Railway автоматически определит Python
   - Если нет, добавьте `Procfile` (см. ниже)

5. **Добавьте переменные окружения**
   - В настройках проекта → Variables
   - Добавьте: `TELEGRAM_BOT_TOKEN=ваш_токен`

6. **Готово!** Бот автоматически запустится

---

### 2. Render (тоже очень простой)

**Шаги:**

1. **Загрузите проект на GitHub**

2. **Создайте аккаунт на Render**
   - https://render.com
   - Зарегистрируйтесь через GitHub

3. **Создайте новый Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

4. **Настройки:**
   - **Name:** crypto-bot (или любое имя)
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python crypto_bot.py`
   - **Python Version:** Убедитесь, что используется Python 3.11 (файл `runtime.txt` уже создан)

5. **Добавьте переменные окружения**
   - В разделе Environment Variables
   - Добавьте: `TELEGRAM_BOT_TOKEN` = `ваш_токен`

6. **Нажмите "Create Web Service"**

**⚠️ Если возникла ошибка AttributeError (Python 3.13 несовместим):**

1. **Убедитесь, что файл `runtime.txt` загружен на GitHub:**
   - Файл должен содержать: `python-3.11`
   - Загрузите изменения на GitHub (git add, commit, push)

2. **На Render очистите кэш и пересоберите:**
   - Перейдите в настройки вашего сервиса
   - Manual Deploy → Clear build cache & deploy
   - Или удалите и создайте сервис заново

3. **Альтернатива: используйте Railway вместо Render**
   - Railway лучше работает с Python версиями
   - Меньше проблем с совместимостью

---

### 3. Fly.io (хорошая альтернатива)

Требует установки CLI, но тоже бесплатный tier.

---

## 📁 Необходимые файлы для деплоя

**Уже созданы:**
- ✅ `Procfile` - для Railway и других платформ
- ✅ `runtime.txt` - для указания версии Python 3.11 (важно для Render!)

Файлы уже созданы в репозитории.

---

## 🔧 Альтернатива: Webhook для Vercel (не рекомендуется)

Если все же хотите использовать Vercel, нужно:

1. Переделать бота на webhook режим (вместо polling)
2. Создать serverless функцию
3. Настроить webhook URL в Telegram

**Ограничения:**
- Job queue (проверка алертов каждые 60 секунд) не будет работать надежно
- Сложнее настройка
- Vercel бесплатный tier имеет лимиты

**Не рекомендую этот вариант для вашего бота!**

---

## 🎯 Рекомендация

**Используйте Railway** - это самый простой и надежный вариант для вашего бота!

1. Загрузите код на GitHub
2. Подключите Railway к репозиторию
3. Добавьте токен в переменные окружения
4. Готово!

Бот будет работать 24/7 бесплатно (с ограничениями бесплатного tier).

